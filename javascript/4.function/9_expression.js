//일반함수, 함수표현식(익명함수), 화살표 (Arrow), 함수
//일반함수 : function 함수명(파라미터, ...) {실행문; }
//함수 표혐식 : let 함수명 = function (파라미터, ...){ 실행문; }
//화살표함수 : let 함수명 => (파라미터) => {실행문; }

/**일반함수 정의시 let, const, 사용 불가 */
function add() {
  return 1 + 2;
}
